namespace UsersManager.Server
{

    // ============================================================
    // משתמש מצומצם לצורכי הצפנת סיסמה בלבד.
    //
    // PasswordHasher של ASP.NET דורש אובייקט משתמש כדי לגזור
    // את ה-hash, ולכן קיימת כאן מחלקה מינימלית במקום להעביר
    // אליו את מחלקת המשתמש המלאה. אינה עוברת ללקוח לעולם
    // ============================================================
    public class MinimalUser
    {
        public string Email { get; set; }

        // הסיסמה המוצפנת כפי שהיא שמורה בבסיס הנתונים.
        // במסלול ההתחברות דרך פורטלם השדה אינו בשימוש,
        // כי הזיהוי נעשה מול הטוקן ולא מול סיסמה מקומית
        public string PasswordHash { get; set; }

    }
}
